import { LocalizationKeys } from "./constant";

//Adding a predefined type here to make sure, We are not forgetting any keys
const en: LocalizationKeys = {
  login: "Login",
  // balance:"User balance is: {{balance}} "
};

export default en;
